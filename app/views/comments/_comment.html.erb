<% if comment.user %>
<h4>Reply: </h4>
<p><%= comment.body %> </p>
<p>User: <%= link_to( comment.user.name, "/users/#{comment.user.id}" ) %> <%= image_tag comment.user.image %>
Created: <%= time_ago_in_words(comment.created_at) %> ago.</p>

<!-- If comment.user.id == @current_user <p>
  <#%= link_to 'Destroy Comment', ([@question, @comment]), method: :delete, data: { confirm: 'Are you sure?'} %>
</p> -->
<% end %>
